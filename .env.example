# 🔐 Clé secrète pour la signature JWT ou autres usages sécurisés
SECRET_KEY=change_this_to_secure_key

# 🗄️ URL de la base de données (PostgreSQL recommandé)
DATABASE_URL=postgresql://USERNAME:PASSWORD@HOST:PORT/DBNAME

# 🌍 Langue par défaut pour l’interface et les réponses
I18N_DEFAULT_LANG=fr

# 📄 Activation de la génération PDF (rapports, certificats)
ENABLE_PDF=true

# 🧾 Activation du middleware d’audit (journalisation des requêtes)
ENABLE_AUDIT=true

# 🐍 Version Python utilisée (optionnel, pour Render)
PYTHON_VERSION=3.11

# 📦 Chemin racine des modules Python (utile pour Render et Uvicorn)
PYTHONPATH=src.env.example
