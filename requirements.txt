# 🔒 Dépendances principales figées

numpy==2.1.3
pandas==2.2.3
scikit-learn==1.5.2
requests==2.32.3
tqdm==4.67.1

# IA offline (sans Torch)
transformers==4.46.2
sentence-transformers==3.3.1
faiss-cpu==1.13.1

# Sécurité / compatibilité
certifi==2025.11.12
regex==2025.11.3
fsspec==2025.12.0

# ⚠️ Torch retiré pour éviter GPU
# Torch CPU-only est installé séparément via Makefile et _install.yml