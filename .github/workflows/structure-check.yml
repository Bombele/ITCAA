name: Structure & Tests Check

on:
  push:
    branches:
      - main
      - feature/ai-offline-module
  pull_request:
    branches:
      - main
      - feature/ai-offline-module
  schedule:
    - cron: "0 2 * * 1"   # Tous les lundis à 02h00 UTC
  workflow_dispatch:

jobs:
  validate-structure-and-tests:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 📦 Installer Poetry
        run: pip install poetry

      - name: 📦 Export requirements depuis pyproject.toml
        run: |
          poetry export -f requirements.txt --without-hashes -o requirements.txt
          poetry export -f requirements.txt --without-hashes --with dev -o requirements-dev.txt

      - name: 📂 Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt', 'requirements-dev.txt', 'src/itcaa_ai_offline/requirements-ai.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: 📦 Installer les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -r src/itcaa_ai_offline/requirements-ai.txt

      - name: 🔧 Charger l’environnement de test
        run: cp .env.test.example .env

      - name: 🎯 Lint avec Black, Isort et Mypy
        run: |
          black --check src/ tests/ | tee logs/black.log
          isort --check-only src/ tests/ | tee logs/isort.log
          mypy --config-file=mypy.ini src tests | tee logs/mypy.log

      - name: 🧪 Lancer les tests avec couverture
        run: pytest tests/ --cov=itcaa_ai_offline --cov-report=xml --cov-report=term-missing | tee logs/pytest.log

      - name: 📊 Vérifier le rapport de couverture
        run: |
          if [ ! -f coverage.xml ]; then
            echo "❌ Rapport de couverture introuvable"
            exit 1
          else
            echo "✅ Rapport de couverture généré"
          fi

      - name: 🗂️ Archiver le rapport de couverture
        uses: actions/upload-artifact@v3
        with:
          name: coverage-report-${{ github.run_number }}
          path: coverage.xml
          if-no-files-found: error

      - name: 📁 Archiver les logs
        uses: actions/upload-artifact@v3
        with:
          name: lint-and-test-logs-${{ github.run_number }}
          path: logs/
          if-no-files-found: ignore

      - name: 🧹 Rotation des rapports (conserver 7 derniers)
        run: |
          mkdir -p public/coverage_reports
          cp coverage.xml public/coverage_reports/coverage_${{ github.run_number }}.xml || true
          ls -t public/coverage_reports | tail -n +8 | xargs -r rm -f

      - name: 🧹 Nettoyage final
        run: |
          rm -rf .pytest_cache __pycache__ */__pycache__ *.pyc *.pyo *.pyd *.log htmlcov/ coverage.xml