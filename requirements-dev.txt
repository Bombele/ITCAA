# 🔒 Dépendances de développement figées (exportées via poetry.lock)

# Outils de qualité et vérification
black==24.10.0
flake8==7.1.1
mypy==1.13.0
isort==5.13.2
pytest==8.3.3
pytest-cov==5.0.0

# Outils de documentation et packaging
sphinx==8.1.3
sphinx-rtd-theme==3.0.1
pre-commit==4.0.1

# IA (sans Torch, géré séparément en CPU-only)
transformers==4.46.2
sentence-transformers==3.3.1
faiss-cpu==1.13.1
scikit-learn==1.5.2

# Sécurité / compatibilité
certifi==2025.11.12
regex==2025.11.3
fsspec==2025.12.0

# ⚠️ Torch retiré pour éviter GPU
# Torch CPU-only est installé séparément via Makefile et _install.yml