name: ITCAA Full CI/CD

on:
  push:
    branches: [ "main" ]
    paths:
      - "src/itcaa_ai_offline/data/corpus/*.txt"
      - "models/model.pt"
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: "0 3 * * 0"   # Tous les dimanches à 03h00 UTC
  workflow_dispatch:

jobs:
  validate-index:
    name: 🔍 Validation index FAISS
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ "3.9", "3.10", "3.11" ]
    steps:
      - name: ✅ Checkout repository
        uses: actions/checkout@v3

      - name: 🐍 Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: 📂 Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt', 'requirements-ai.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: 📦 Installer les dépendances IA + globales
        run: |
          pip install -r requirements.txt
          pip install -r requirements-ai.txt
        shell: bash

      - name: 🔍 Vérification installation
        run: pip list
        shell: bash

      - name: 🧠 Set PYTHONPATH
        run: echo "PYTHONPATH=$(pwd)/src" >> $GITHUB_ENV
        shell: bash

      - name: 🔍 Vérification structure IA
        run: python scripts/check_structure.py
        shell: bash

      - name: 🧩 Validate dependencies
        run: python validate_dependencies.py
        shell: bash

      - name: 🛠️ Repair FAISS index if needed
        run: python repair_index.py
        shell: bash

      - name: 🧬 Rebuild FAISS index (incremental)
        run: python src/itcaa_ai_offline/data/corpus/index_builder.py --incremental
        shell: bash

      - name: 🧪 Run tests with coverage
        run: pytest -v --cov=src/itcaa_ai_offline --cov-report=xml --cov-report=term-missing tests/
        shell: bash

      - name: 📊 Vérifier le rapport de couverture
        run: cat coverage.xml | grep "<coverage"
        shell: bash

      - name: 📁 Upload coverage report artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml

      - name: 📊 Generate index report
        run: python generate_index_report.py
        shell: bash

      - name: 📁 Upload index report artifact
        uses: actions/upload-artifact@v4
        with:
          name: index-report
          path: src/itcaa_ai_offline/data/index/index_report.md

  validate-model-and-predictor:
    name: 🧠 Validation modèle et prédicteur
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ "3.9", "3.10", "3.11" ]
    steps:
      - name: ✅ Checkout repository
        uses: actions/checkout@v3

      - name: 🐍 Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: 📂 Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt', 'requirements-ai.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: 📦 Installer les dépendances IA + globales
        run: |
          pip install -r requirements.txt
          pip install -r requirements-ai.txt
        shell: bash

      - name: 🔍 Vérification installation
        run: pip list
        shell: bash

      - name: 🧠 Set PYTHONPATH
        run: echo "PYTHONPATH=$(pwd)/src" >> $GITHUB_ENV
        shell: bash

      - name: 🔍 Vérification structure IA
        run: python scripts/check_structure.py
        shell: bash

      - name: 🧩 Validate dependencies
        run: python validate_dependencies.py
        shell: bash

      - name: 🧪 Run model loader tests with coverage
        run: pytest -v --cov=src/itcaa_ai_offline --cov-report=xml --cov-append tests/test_model_loader.py
        shell: bash

      - name: 🧠 Run predictor tests with coverage
        run: pytest -v --cov=src/itcaa_ai_offline --cov-report=xml --cov-append tests/test_predictor.py
        shell: bash

      - name: 📁 Upload coverage report artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml