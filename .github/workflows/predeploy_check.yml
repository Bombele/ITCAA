name: Predeploy Check

on:
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  predeploy-check:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4

      - name: 🐍 Installer Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📂 Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt', 'requirements-dev.txt', 'src/itcaa_ai_offline/requirements-ai.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: 📦 Installer les dépendances (Dev)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -r src/itcaa_ai_offline/requirements-ai.txt

      - name: 🎯 Lint avec Black et Isort
        run: |
          black --check src/ tests/ || (echo "❌ Black a trouvé des erreurs" && exit 1)
          isort --check-only src/ tests/ || (echo "❌ Isort a trouvé des erreurs" && exit 1)

      - name: 🔎 Vérification stricte des types avec Mypy
        run: |
          mypy src tests || (echo "❌ Mypy a trouvé des erreurs" && exit 1)

      - name: 🧪 Lancer les tests unitaires
        run: |
          pytest tests/ --maxfail=1 --disable-warnings || (echo "⚠️ Tests échoués" && exit 1)

      - name: ✅ Vérification import API
        run: |
          python test_import.py || (echo "❌ Import API échoué" && exit 1)